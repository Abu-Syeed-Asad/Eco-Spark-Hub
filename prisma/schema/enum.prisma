enum USER_STATUS {
  ACTIVE
  DELETE
  BLOCK
}

enum ROLE {
  USER
  ADMIN
}

enum POST_TYPE {
  FREE
  PAID
  UNPAID
}

enum STRIPE_PAYMENT_STATUS {
  PAID
  UNPAID
}

enum POST_STATUS {
  DRAFT
  APPROVED
  REJECTED
}

enum VoteType {
  UPVOTE
  DOWNVOTE
}

enum FINANCE_SOURCE {
  POST
  DIPOSIT
  REWORD
  EAEN
}

enum ACCOUNT_TYPE {
  USER
  COMPANY
}
